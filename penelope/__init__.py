"""Penelope - a self-hosted agent runtime.

A streaming tool-calling loop whose agents, skills, and system prompts live in a
database and are edited from a web UI. Coding work is delegated to pluggable coding
CLIs through the CodingExecutor interface rather than reimplemented.

This package is a scaffold: interfaces are defined, the loop is not yet built.
"""

__version__ = "0.0.0"
